apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-pg
  namespace: ecommerce-devops
spec:
  storageClassName: "hostpath"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi